services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: nextjs
    container_name: blog
    ports:
      - 3000:3000
    networks:
      - nginx-minio-network

networks:
  nginx-minio-network:
    external: true
